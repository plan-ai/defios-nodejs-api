import { IDefaultVestingScheduleChanged } from '../events'

export const defaultVestingScheduleChanged = async (
    req: IDefaultVestingScheduleChanged
) => {
    return new Promise(async (resolve, reject) => {})
}
