import { IAddObjectiveDataEvent } from '../events'
import { Roadmap , RoadmapObjective} from '../models/roadmap'

export const addObjectiveData = async (res: IAddObjectiveDataEvent) => {
    return new Promise(async (resolve, reject) => {

        }
)}
